{
    "builds": [
        {
            "src": "backend/server.ts",
            "use": "@vercel/node",
            "config": {
                "nodeVersion": "20.x"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "backend/server.ts"
        },
        {
            "src": "/(.*)",
            "dest": "backend/server.ts"
        }
    ],
    "env": {
        "NODE_ENV": "production"
    }
}
